{
  "version": 2,
  "builds": [
    {
      "src": "server/package.json",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "server/dist"
      }
    },
    {
      "src": "server/dist/src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/server/dist/(.*)", "dest": "/server/dist/$1" },
    { "src": "/(.*)", "dest": "/server/dist/src/index.js" }
  ]
}
